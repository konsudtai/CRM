{
  "name": "sf7-agentcore",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/app.ts --bundle --platform=node --target=node22 --outfile=dist/app.js --external:@aws-sdk/*",
    "package": "cd dist && zip -r ../agent.zip app.js"
  },
  "dependencies": {
    "@strands-agents/sdk": "^0.1.0",
    "express": "^5.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
